{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "push": "npx lerna publish from-package --yes"
  }
}
